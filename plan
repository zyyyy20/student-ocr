学生成绩单旋转图像预处理方案调研（不改代码）

背景
- 现有图片存在一致的旋转角度（非水平/垂直），会导致表头/数字漏检与表格结构不稳
- 目标是在进入 OCR/表格识别前做“去倾斜/纠偏”，提升识别准确率与稳定性

目标
- 自动估计旋转角度（±15° 或更大可扩展）
- 去倾斜后保证文本清晰、表格线对齐
- 可批量处理，速度可控
- 对不同分辨率/噪声/压缩质量保持鲁棒

方案一：基于文本行投影的倾斜角估计（Projection Profile）
思路
- 将图像二值化（自适应阈值或 Otsu）
- 旋转若干角度（例如 -10°~+10°，步长 0.2°）
- 计算水平投影（每行像素和），方差/峰度最大时为最佳角
优点
- 对纯文本/表格都有效
- 实现简单，可控
缺点
- 需要角度搜索，耗时随步长增长
- 需要二值化质量较好
适用
- 旋转角不大且整体文本密集
建议参数
- 角度范围：±10°（若未知，可扩展到 ±20°）
- 步长：0.2° 或 0.5°

方案二：基于 Hough 直线检测的倾斜角估计
思路
- Canny 边缘 + HoughLines/HoughLinesP
- 统计直线角度直方图，取主峰为倾斜角
- 对表格线/文本行有效
优点
- 对有表格线的成绩单很有效
- 速度快，不需要大范围扫描
缺点
- 图像噪声大或线条断裂时不稳定
- 无明显直线时失败
适用
- 有明显网格线或表格边框
建议参数
- Canny 阈值需与图像对比度匹配
- Hough 累计阈值适中，避免噪声线

方案三：基于最小外接矩形（minAreaRect）的文档纠偏
思路
- 二值化后找最大连通域（或轮廓）
- 用 minAreaRect 得到整体倾斜角
优点
- 实现简单，速度快
缺点
- 背景杂物/阴影会干扰
- 图片裁剪不干净时偏差大
适用
- 文档边缘清晰、背景干净

方案四：基于文本方向分类器（OCR Angle Classifier）
思路
- 使用 OCR 模型自带的方向分类（如 PaddleOCR 的 angle cls）
- 先做方向预测，再旋转纠正
优点
- 与 OCR 生态一致，集成成本低
- 对较大旋转角（90/180）表现好
缺点
- 通常只判 0/90/180/270，不适合小角度倾斜
适用
- 角度接近 90°/180° 的极端情况

方案五：基于 Radon 变换的倾斜角估计
思路
- 对二值图做 Radon 变换
- 能量最大方向对应文本行方向
优点
- 对斜体/细线文本较鲁棒
缺点
- 实现复杂，计算较慢
适用
- 高精度需求，批量量级不大

方案六：基于频域（Fourier）方向能量估计
思路
- 对图像做傅里叶变换
- 频谱中主方向能量对齐文本/表格方向
优点
- 对周期性网格线有效
缺点
- 解释与实现成本较高，调参复杂
适用
- 有明显网格/规则纹理的表格

推荐组合策略（实用优先）
1) 先尝试 Hough 直线检测（表格线明显时最快最准）
2) 若失败或置信度低，再用投影法精细扫描
3) 对边缘清晰的扫描件可并行尝试 minAreaRect
4) Angle Classifier 仅用于极端旋转（90/180）

质量评估与验收指标
- OCR 结果：表头完整率、数字漏位率（如 99->9）
- 表格结构：列数正确率、行数正确率
- 置信度：低置信度单元格比例下降

需要准备的样本
- 当前旋转图：C:\Users\Admin\Desktop\student-ocr\Snipaste_2026-03-12_11-47-14.png
- 3~5 张类似角度的样本
- 1~2 张未旋转样本（对比基线）

下一步（如要落地）
- 确定目标角度范围与精度需求
- 选定方案与参数，做小样本验证
- 再决定是否接入后端预处理链路
